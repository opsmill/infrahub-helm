Bumped the `infrahub` dependency to 4.30.0, which adds `global.infrahubImageFlavor` to select a flavored Infrahub image (for example `avd`) without pinning the image tag. When set, the flavor is appended to the resolved image tag as `<tag>-<flavor>`, whether the tag comes from `global.infrahubTag` or defaults to the chart `appVersion`, so upgrading the chart alone is enough to track new versions.
